# DragonLeaderStrategy 数据源 TODO

- AKShare 已接入第一版日线与实时行情列表；当前默认 `tracked` 范围，同步任务已支持进度状态、失败重试、失败补抓队列和增量日线状态。全 A 股逐只历史日线仍需继续拆成后台 backfill 批次。
- 行业字段：第一版 AKShare 同步默认沿用本地行业字段；如开启行业成分映射，需要处理请求耗时和上游限流。
- FLUM 行业映射：当前已新增 `stock_industry_map` 和关键词降级映射，仍需接入真实申万一级/二级/三级行业历史分类，避免“行业待映射”影响行业热度与回测一致性。
- 精确涨停价：当前按市场和 ST 状态使用近似涨停阈值，后续需要接入交易所涨跌停价或行情源字段。
- ST / *ST / 退市整理 / 停牌状态：AKShare 第一版按名称和实时行情字段近似识别，退市整理状态仍需专门证券状态字段。
- 上市日期：当前仍沿用本地 `list_date` 或日线最早日期，后续需要接入真实上市日期。
- 概念板块：当前使用行业代替概念板块，后续需要接入概念、题材、热门板块及成分股关系。
- 分时与封单数据：`firstLimitTime`、`openLimitCount`、`sealAmount`、`sealAmountRatio` 暂无真实数据，封板质量使用默认估计。
- 市场指数数据：当前用样本股票均值近似大盘涨跌和 5/20 日收益，后续需要接入沪深主要指数。
- 昨日涨停股今日平均收益：当前基于样本库近似计算，后续需要用全市场涨停样本。
- VWAP：当前回测优先使用次日开盘价，后续可接入分钟成交数据计算次日 VWAP。

# 任务与性能优化 TODO

- 回测缓存：需要增加 `strategy + 参数 hash + 数据版本 + 日期区间` 的缓存命中逻辑，避免相同参数重复完整回测。
- 增量回测：当前异步回测任务已不阻塞页面，但仍复用现有完整回测计算；后续需要支持新增交易日追加计算。
- 快照先行：需要新增 `stock_snapshot_daily` 和行情快照批量 upsert，让 MarketRegime 与 MarketTheme 先用快照快速生成。
- 因子缓存：需要新增 `stock_factors_daily`，按 `last_factor_date` 跳过未变化股票。
- 性能分析页：需要汇总 `task_runs.summary_json` 中的阶段耗时，展示最近 10 次任务耗时趋势、最慢阶段、失败率和平均重试次数。

# 业界经典量化策略数据源 TODO

- 财务因子：`pe_ttm`、`pb`、`ps`、股息率、自由现金流收益率、ROE、ROA、毛利率、净利率、现金流覆盖净利润、资产负债率、收入/利润同比增长等第一版使用缺失容错代理值；后续需要接入按公告日期生效的真实财务数据。
- 市场基准：MarketRegimeModel 当前用本地股票池近似全市场、沪深300或中证全指；后续需要接入指数行情、全市场涨跌家数、真实涨停/跌停统计和成交额。
- 行情快照：当前对 2026-05-06 使用手工行情快照修正本地样本股票池偏差；后续需要接入实时指数涨跌幅、全市场成交额、上涨/下跌家数、涨停/跌停数量和风格指数强度，替代手工快照。
- Beta / downsideCapture：当前基于波动率代理估计，后续需要用指数收益序列计算 120 日 beta、下行捕获率。
- AlphaLab：第一版只提供 10 个价量 Alpha 目录；后续需要实现单因子分组回测、IC、RankIC、分组收益、换手率、最大回撤、多空收益和有效性评分。
- PortfolioRiskBudget：第一版基于候选元数据做规则预算；后续需要引入协方差矩阵、行业约束优化和策略置信度历史校准。
- ResearchIntegrityChecker：第一版为规则检查清单；后续需要对回测任务自动记录参数版本、数据版本、运行时间，并检测未来函数和幸存者偏差。

# MarketHotspotStrategy 数据源 TODO

- 需要接入概念题材、涨停板、炸板、最强板块统计数据源；当前第一版使用行业热度替代题材热度。
- 需要接入 Tushare `limit_list_d`：A 股每日涨跌停、炸板数据，用于区分涨停、跌停、炸板未回封和短线情绪退潮。
- 需要接入 Tushare `limit_cpt_list`：涨停最强概念板块，用于计算真正的市场主线、题材热度和概念延续性。
- 需要接入 Tushare `limit_list_ths`：同花顺涨停池、连板池、炸板池，用于补充连板高度、连板家数和短线强势池。
- 可补充 AKShare A 股行情、行业和实时行情接口，完善成交额、行业涨幅、实时涨跌停近似字段。
- 短线强势池：后续可接入东方财富涨停板、昨日涨停、强势股、炸板股、跌停股等页面，补充 `isLimitUpBroken`、最先封板时间、炸板次数和封单强度。
- 概念板块轮动：后续可接入 Tushare `limit_cpt_list` 或等价数据源，获取每日涨停最多、最强概念板块及成分股关系。
- 涨跌停明细：后续可接入 Tushare `limit_list_d` 或等价数据源，补充每日涨跌停、炸板、连板高度和短线情绪统计。
